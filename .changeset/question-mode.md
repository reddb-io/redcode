---
"@reddb-io/redcode": minor
---

Add a fourth built-in mode: `question`. It can only read — its mission is to explain how something works, citing files and line numbers, never writing code or editing files. Selectable alongside build, plan and design.