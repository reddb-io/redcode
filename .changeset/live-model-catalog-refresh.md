---
"@reddb-io/redcode": patch
---

Reload providers and models live when the models catalog refreshes, so newly published models (for example OpenRouter's GLM-5.3-Flash) show up in `/models` without restarting, and allow the catalog download up to 30 seconds instead of 10.
