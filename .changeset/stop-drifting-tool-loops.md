---
"@reddb-io/redcode": patch
---

Stop provider turns that repeat the same progress update and unchanged tool result while varying command syntax. Preserve polling when tool results continue to change.
