---
"@reddb-io/redcode": patch
---

Use `»` between router and upstream hops in model route labels (TUI `/model`, `/setup`, web settings and model pickers), keeping `·` only before the model name, so chains like `RedRouter » RedRouter » OpenCode Zen (via OpenCode Go) · JEV 1.13` are easier to scan.
