---
"@reddb-io/redcode": patch
---

Expose Design preview arguments as an object schema compatible with model tool calling, while preserving validation of current IDs and legacy prototype paths. Return the exact preview call with each design document and explain how to recover missing arguments instead of repeating an empty call.
