---
"@reddb-io/redcode": patch
---

The TUI context sidebar shows only the current step's latency and tokens per second. The time-to-output breakdown, the turn totals and the aborted, burst and reasoning-hidden markers are gone; when there is no real rate, nothing is shown instead of an explanation.
