---
"@reddb-io/redcode": patch
---

Make the steered indicator on user messages a subtle muted marker instead of a yellow "steered" text line, since steering is now the normal way mid-turn messages are sent. Queued messages keep their existing clear indicator.
