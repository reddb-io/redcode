---
"@reddb-io/redcode": patch
---

A single-line paste now stays as visible text up to 250 characters (was 150) before the prompt folds it into `[Pasted ~N lines]`, so a dictated sentence is readable in the prompt. Pastes of three or more lines are still summarized.
