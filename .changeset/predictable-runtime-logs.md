---
"@reddb-io/redcode": patch
---

Keep local diagnostic logs at their established paths with automatic size rotation, private creation, credential redaction and durable CLI fatal errors. Add `redcode debug logs --path` and `--open` for predictable troubleshooting and desktop log access.

Bound desktop log history and include the current RedCode log location in desktop debug exports without dropping legacy sources.
