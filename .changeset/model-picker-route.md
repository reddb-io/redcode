---
"@reddb-io/redcode": patch
---

Show a model's router route as a title suffix in `/model` when another catalog entry has the same name, and widen the `/model` and provider dialogs so routed model names and descriptions stop getting cut off.
